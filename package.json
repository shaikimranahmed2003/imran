{
  "name": "tgnpdcl-employee-api",
  "version": "1.0.0",
  "description": "Employee self-service API for the TGNPDCL Payroll app (no dependencies)",
  "main": "server.js",
  "scripts": { "start": "node server.js" },
  "engines": { "node": ">=18" }
}
